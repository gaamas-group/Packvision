import React from 'react';

const LoginPage = () => {
  return (
    <div>
      <h1>Login</h1>
      <button>Login as Admin</button>
      <button>Login as Packager</button>
    </div>
  );
};

export default LoginPage;
